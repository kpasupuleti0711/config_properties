spring:
  application:
    name: WELCOME-API
    
  boot:
    admin:
      client:
        url: http://localhost:1111
   
StubProcess1: KOTI-WELCOME
    
eureka:
 client:
  service-url:
   defaultZone: http://localhost:8761/eureka/
    
management:
  endpoints:
    web:
      exposure:
        include: '*'
    
#    eureka:
#      client:
#        register-with-eureka: true
#        
#      eureka:
#         client:
#           fetch-registry: true